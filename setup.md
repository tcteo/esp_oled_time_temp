# Project setup

```shell
# Set up `uv` project
uv init

# Add PlatformIO to the venv
uv add platformio

# Initialize the PlatformIO project
uv run pio project init --board d1_mini

# Create source file
touch src/main.cpp
# ... and edit
```
